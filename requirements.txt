Django>=5.0,<5.2
djangorestframework>=3.15,<3.16
django-cors-headers>=4.3,<5
django-environ>=0.11,<0.13
psycopg[binary]>=3.1,<4
cryptography>=42
gunicorn>=22
whitenoise>=6.6
# Profile photo uploads to the same B2 bucket LinkedTrust uses for claim media.
# B2 speaks S3; unused (and unimported) when GOVKIT_STORAGE_* is unset.
boto3>=1.34,<2
# Shrinks uploaded pictures before they go in the bucket (apps.commons.storage).
# Optional: without it, pictures are stored exactly as they arrived.
Pillow>=10.3,<12
